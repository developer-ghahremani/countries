import { BsFillSunFill } from "react-icons/bs";
import { IconBaseProps } from "react-icons";

interface Props extends IconBaseProps {}

const Sun = (props: Props) => <BsFillSunFill {...props} />;

export default Sun;
