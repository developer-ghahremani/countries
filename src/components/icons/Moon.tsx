import { BsMoonFill } from "react-icons/bs";
import { IconBaseProps } from "react-icons";

interface Props extends IconBaseProps {}

const Moon = (props: Props) => <BsMoonFill {...props} />;

export default Moon;
